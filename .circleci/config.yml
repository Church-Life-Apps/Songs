version: 2.1
orbs:
  node: circleci/node@3.0.0

jobs:
  build:
    working_directory: ~/Songs
    executor: node/default
    steps:
      - checkout
      - node/install-npm
      - node/install-packages:
          app-dir: ~/Songs
          cache-path: node_modules
          override-ci-command: npm i
      - persist_to_workspace
          root: .
          paths:
            - .

  docker-test:
    docker:
      - image: cimg/node:current
      - run:
          name: Test
          command: npm test


workflows:
  build-and-test:
    jobs:
      - build
      - docker-test
      - node/test
